{
  "_comment": "Prompt config for the 'recommend_next' AI operation.",
  "_meta": {
    "purpose": "Pick the single best next concept node for the user to study, respecting prerequisite ordering (all incoming edges must be 'learned' or 'skipped') and preferring nodes on the shortest critical path to the target node.",
    "provider": "OpenAI-compatible (e.g. Kimi 2.5, OpenAI GPT-4o)",
    "key_params": {
      "temperature": "0.5 — balanced: some personalisation without wild variation",
      "max_tokens": "1024 — intentionally small; output is just one node ID and one sentence"
    },
    "input_format": "JSON string with keys: graph (nodes+edges), user_profile, learning_history (learned_nodes, skipped_nodes), learning_goal",
    "consumed_by": "services/ai_service.py AIService.recommend_next()"
  },
  "model_params": {
    "temperature": 0.5,
    "max_tokens": 1024
  },
  "system_prompt": "You are a learning path advisor. Given a user's knowledge graph, learning history, and profile, recommend the single best next node to study. Prioritize nodes on the critical path to the target, where all prerequisites are completed.",
  "output_format": {
    "recommended_node_id": "The id of the recommended node, or null if all nodes are completed",
    "reason": "One sentence in Chinese explaining why this node is recommended"
  },
  "rules": [
    "Only recommend nodes with status 'unlearned'",
    "Only recommend nodes whose ALL prerequisite nodes (incoming edges) have status 'learned' or 'skipped'",
    "Among eligible nodes, prefer nodes on the shortest path to the target node",
    "Consider user profile: if math_level is weak, prefer foundational nodes over advanced ones",
    "If all nodes are completed or skipped, return recommended_node_id: null",
    "The reason MUST reference why this specific node is the best choice now (e.g., prerequisites completed, critical path)"
  ],
  "examples": [
    {
      "input": "Graph has nodes: n1(矩阵乘法, learned), n2(链式法则, unlearned, prereq: n1), n3(梯度下降, unlearned, prereq: n2). Target: n4(反向传播). User learned n1.",
      "output": {"recommended_node_id": "n2", "reason": "链式法则的前置知识矩阵乘法已掌握，且是通往反向传播的关键路径"}
    },
    {
      "input": "All nodes are learned or skipped.",
      "output": {"recommended_node_id": null, "reason": "所有知识点已完成，学习目标达成"}
    }
  ]
}
